{
  "name": "website-boilerplate25",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch scss:css --style=compressed",
    "serve": "browser-sync start --server --files 'css/*.css, *.html'",
    "start": "npm-run-all --parallel sass serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "npm-run-all": "^4.1.5",
    "sass": "^1.87.0"
  }
}
